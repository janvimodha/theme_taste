{% if section.settings.Product_per_page !=0 %}
    {% for number in collection %}
        {% if number == product_per_page %}
            {{number}}
        {% endif %}
    {% endfor %}
{% endif %}

{% schema %}
{
    "name":"featured product demo",
    "settings":[
    {
          "type":"number",
          "id":"product_per_page",
          "label":"choose product to display",
          "default":3
    }
    ],
    "presets":[{
        "name":"featured product demo"
    }]
}
{% endschema %}